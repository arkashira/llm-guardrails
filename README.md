# Guardrails

A Python project for managing guardrail violations.

## Usage

1. Create a `Guardrails` instance with an organization scope.
2. Add violations using the `add_violation` method.
3. Export violations in CSV format using the `export_violations` method.
4. Filter violations by date range using the `filter_violations` method.
